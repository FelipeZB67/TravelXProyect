import { ReservaInternacionalModel } from './reserva-internacional.service';

describe('ReservaInternacional', () => {
  it('should create an instance', () => {
    expect(new ReservaInternacional()).toBeTruthy();
  });
});
