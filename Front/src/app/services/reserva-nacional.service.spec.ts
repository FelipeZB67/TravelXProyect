import { ReservaNacionalService } from './reserva-nacional.service';

describe('ReservaNacionalService', () => {
  it('should create an instance', () => {
    expect(new ReservaNacionalService()).toBeTruthy();
  });
});
